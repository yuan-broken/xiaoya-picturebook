# 小芽绘本 v1.0 开发日志

> 记录每次代码更新，按日期倒序排列。新增条目请加在最新日期下方。

---

## 2026-09-15（家长-孩子数据同步逻辑检查 & 孩子登录修复）

### 检查范围
全面检查家长账号与孩子的数据同步链路：数据库表、注册/登录、添加孩子、查询列表、JWT 身份区分、前端数据调用。

### 检查结果（后端全部正确）
1. 数据库：`biz_family`（家长）与 `biz_child_profile`（孩子）通过 `family_id` 外键关联 ✅
2. 家长注册/登录：正确读写 `biz_family`，JWT role=parent ✅
3. 孩子登录：查 `biz_child_profile`，校验 `bind_status=1`，JWT role=child ✅
4. 添加孩子：`familyId` 从 JWT 解析（`currentFamilyId()`），不再兜底到演示账号 ✅
5. 查询孩子列表：`listChildren(familyId)` 按 familyId 精确过滤 ✅
6. JwtAuthFilter：parent/child 角色分离，details 存对应 ID ✅

### 发现并修复的 Bug
**child-login.html 孩子登录完全不可用**：
- 代码调用 `api.auth.childLogin`、`setToken`、`setUser`，但 api.js 导出的是 `API` 对象（`API.auth.childLogin`、`API.setToken`、`API.setUser`）
- 导致孩子登录时报 `api is not defined` 错误，无法保存 token，家长看不到孩子阅读数据
- 修复：全部改为 `API.xxx`，并补充 `familyId`、`memberLevel`、`memberEndAt` 字段到 localStorage

### 验证
- API 全链路：家长 familyId=3 → 添加孩子 familyId=3 → 孩子登录 familyId=3，关联一致 ✅
- 浏览器：孩子账号 kid_sync 登录成功，跳转到 reader.html ✅

---

## 2026-09-15（头像生成内容匹配 & 每次不同）

### 问题
- 相同输入每次生成相同图片（pollinations.ai 无 seed 参数导致缓存命中）
- 头像内容与输入不匹配（prompt 中"角色头像"干扰，后端又叠加风格后缀）

### 修复
1. **pollinations.ai URL 加随机 seed**：`seed = System.currentTimeMillis() % 1000000 + random(1000000)`，确保每次生成不同图片
2. **后端去掉重复风格后缀**：直接使用前端传入的 prompt，不再叠加 `cute cartoon character avatar...` 避免干扰内容匹配
3. **前端 prompt 优化**：`Q版可爱的{角色名}，chibi萌系风格...`（去掉"角色头像"，让输入内容作为主体）
   - 输入"向日葵" → 生成 Q 版可爱的向日葵
   - 输入"小老虎" → 生成 Q 版可爱的小老虎

### 验证
- 两次相同输入返回不同 seed（1431862 vs 692092）✅
- 输入"向日葵"生成向日葵相关 Q 版头像 ✅
- 图片 URL 可访问（image/jpeg）✅

---

## 2026-09-15（家长数据持久化 & 孩子关联修复 & Q版头像）

### 家长账号数据库持久化

**问题**：`Family` 实体中大量字段标注 `@TableField(exist = false)`，家长姓名、头像、会员状态、内容筛选配置、时长管控等信息只存在内存，不持久化到数据库。

**修复**：
- `schema.sql`：`biz_family` 表新增 `parent_name`、`avatar`、`member_status`、`member_start`、`whitelist_themes`、`blacklist_themes`、`daily_limit_min`、`single_limit_min`、`anti_addiction_flag` 列
- `Family.java`：移除所有 `@TableField(exist = false)`，字段全部持久化；`memberStartAt` 映射 `member_start` 列
- `biz_child_profile` 表新增 `avatar`、`status` 列

### 修复"未知家庭"问题

**根因**：`UserController.currentFamilyId()` 在 token 无效时兜底返回 `1L`（演示账号），导致添加孩子时关联到错误家庭。

**修复**：
- `UserController`、`OrderController`、`HabitController` 的 `currentFamilyId()` 改为抛出 `"请先登录"` 异常，不再兜底到演示账号
- 孩子的 `family_id` 始终取当前登录家长的 ID，确保关联正确

### 孩子账户只能由家长创建

- 前端无独立孩子注册入口，孩子账号仅可通过家长端"添加孩子"创建
- 后端 `POST /api/user/child` 需家长 JWT 认证，`familyId` 由 `currentFamilyId()` 注入，不可伪造

### AI 头像生成改为 Q 版可爱风格

- `studio.html` 的 `generateAvatar()` prompt 更新为：`Q版可爱卡通{角色名}角色头像，chibi大头萌系风格，圆润大眼睛，儿童绘本插画，柔和马卡龙色彩，正面肖像，纯色简单背景，适合幼儿`
- 确保头像与角色名称一致，生成 Q 版可爱风格

### ChildProfile 字段映射修复

- 添加 `@JsonProperty("birthDate")` 使前端 `birthDate` 参数正确映射到 `birthday` 字段
- `avatar` 字段持久化到数据库

**验证**：注册→登录→查询账号信息→添加孩子→查询孩子列表全链路通过，孩子 familyId 正确关联家长；Q 版头像生成成功 ✅

---

## 2026-09-15（接入故事生成 & AI 头像生成）

### Studio 页面 AI 能力接入

**目标**：为 Studio 页面的"生成我的故事"和"自定义角色 AI 生成头像"接入真实 AI 能力。

**故事生成（LLM）**：
- 复用已有硅基流动 LLM 集成（`Qwen/Qwen2.5-7B-Instruct`）
- 接口：`POST /api/story/create`，参数 `{roleId, roleName, themeId, storyInput}`
- 后端 `AiChatService.createStory()` 构造 prompt 调用 LLM，返回分页故事内容
- 前端 `generateStory()` 调用 `API.story.generate()` 渲染故事

**AI 头像生成（图片）**：
- 接口：`POST /api/ai/image/generate`，参数 `{prompt}`
- 优先调用硅基流动图片 API，失败回退到 **pollinations.ai**（免费无需 key）
- pollinations.ai URL 格式：`https://image.pollinations.ai/prompt/{prompt}?width=1024&height=1024&nologo=true&model=flux`
- 前端 `generateAvatar()` 调用 `API.ai.imageGenerate()` 显示头像图片

**修复项**：
- 修正 `api.js` 中故事接口路径：`/api/chat/story/create` → `/api/story/create`
- `StoryCreation` 实体字段与数据库列名映射（`@TableField` 注解）
- `schema.sql` 的 `biz_story_creation` 表新增 `role_name` 列
- 硅基流动图片模型 `black-forest-labs/FLUX.1-schnell` 被禁用，改用 pollinations.ai 回退

**验证**：故事生成返回 LLM 内容 ✅，头像生成返回 pollinations.ai 图片 ✅

---

## 2026-09-15（改用硅基流动免费 API）

### 切换 LLM 到硅基流动 SiliconFlow（永久免费）

**变更原因**：阿里云百炼免费额度 90 天过期且需支付宝实名，改用硅基流动 — 永久免费 2000 万 Token，手机号注册即可，OpenAI 兼容格式。

**修改**：
- `application.yml`：LLM `api-url` 改为 `https://api.siliconflow.cn/v1`，`model` 改为 `Qwen/Qwen2.5-7B-Instruct`
- TTS/ASR provider 改回 `mock`（后续可接浏览器 Web Speech API 实现完全免费）
- 代码无需修改（已用 OpenAI 兼容格式，`callDashScopeLlm()` 方法兼容所有 OpenAI 格式 API）

**验证**：构建成功 + 后端启动 + 聊天 API 全链路通过（无 Key 时降级 mock）✅

---

## 2026-09-15（接入阿里云百炼 AI）

### AI 真实 API 接入：阿里云百炼 DashScope

**目标**：将 LLM 对话、ASR 语音识别、TTS 语音合成从 Mock 实现改造为调用阿里云百炼 DashScope API，保留 Mock 作为降级方案。

**修改清单**：

| 文件 | 改动 |
|------|------|
| `application.yml` | provider 从 `mock` 改为 `dashscope`；LLM 模型改为 `qwen-turbo`，API 地址改为 `https://dashscope.aliyuncs.com/compatible-mode/v1`；TTS 模型改为 `cosyvoice-v1`，音色改为 `longxiaoxia_v2`；ASR 模型改为 `qwen3-asr-flash` |
| `AiChatService.java` | 1) 新增 `@Value` 注入 LLM 配置（provider/model/api-url/api-key/temperature/max-token）。2) `generateAiReply()` 改为：provider=dashscope 时调用 `callDashScopeLlm()`（OpenAI 兼容格式 POST），失败降级为 `mockAiReply()`。3) `generateStoryContent()` 同样改造。4) 新增 `callDashScopeLlm()`、`parseContentFromResponse()`、`escapeJson()`、`mockAiReply()` 方法 |
| `AsrServiceImpl.java` | 1) 新增 `@Value` 注入 ASR 模型和 DashScope 配置。2) `recognize()` 改为：provider=dashscope 时调用 `callDashScopeAsr()`（音频→Base64→POST），失败降级为 Mock。3) 新增 `callDashScopeAsr()`、`getAudioBase64()`、`parseAsrContent()`、`extractJsonString()` 方法 |
| `TtsServiceImpl.java` | 1) 新增 `@Value` 注入 TTS 模型、音色、DashScope Key、本地存储路径。2) `synthesize()` 改为：provider=dashscope 时调用 `callDashScopeTts()`（CosyVoice RESTful API，返回音频二进制→保存本地 .mp3），失败降级为 Mock。3) 新增 `callDashScopeTts()`、`mapToDashscopeVoice()`、`escapeJson()` 方法 |

**关键设计**：
- 所有 AI 服务采用 **双通道**：provider=dashscope 走真实 API，API 失败时自动降级为 Mock，保证服务可用性
- API Key 通过环境变量 `AI_LLM_KEY` 注入，不硬编码在配置文件中
- LLM 使用 OpenAI 兼容格式（`/compatible-mode/v1/chat/completions`），可直接用 `HttpURLConnection` 调用，无需额外 SDK 依赖
- TTS 返回音频二进制流，保存为本地 .mp3 文件（`/uploads/ai_result/` 目录），前端通过 URL 播放
- ASR 支持本地文件路径和 HTTP URL 两种音频输入方式

**配置方式**：
```bash
# 启动时设置环境变量
$env:AI_LLM_KEY="sk-你的百炼API Key"
java -jar picture-book-backend-1.0.0.jar

# 或不设置 Key（走 mock 降级）
java -jar picture-book-backend-1.0.0.jar
```

**验证结果**：
- `mvn clean package -DskipTests` 构建成功 ✅
- 后端启动正常（端口 8080） ✅
- 注册 + 登录 + 创建对话会话 + 发送消息 + 收到 AI 回复 全链路通过 ✅
- 无 API Key 时正确降级为 Mock 回复 ✅

**待用户操作**：
1. 注册阿里云百炼（[bailian.console.aliyun.com](https://bailian.console.aliyun.com)）
2. 创建 API Key
3. 设置环境变量 `AI_LLM_KEY` 后重启后端即可启用真实 AI

---

## 2026-09-15（傍晚）

### 测试账号：未部署后端时查看页面内容

**问题背景**：后端未部署时无法登录，导致 parent.html、upgrade.html、reader.html 等需登录页面无法查看内容。需要一个测试账号，在后端未运行时也能进入这些页面浏览。

**修改清单**：

| 文件 | 改动 |
|------|------|
| `前端页面/ai-picturebook/login.html` | 1) `doLogin` 函数顶部新增测试账号本地 fallback：当 username=`test`、password=`test123456` 时，直接在本地创建 mock token（`test-token-{timestamp}`）和 mock 用户数据（nickname=测试家长, memberLevel=premium, role=parent），不调后端 API，直接跳转 home.html。2) 登录表单下方新增测试账号提示框（橙色虚线边框）："测试账号：test / test123456（仅未部署后端时使用）"。3) api.js 版本号 v6→v7 破缓存 |
| `前端页面/ai-picturebook/api.js` | `request()` 的 401/403 处理新增 test-token 判断：`if (!token.startsWith('test-token-'))` 才调 clearAuth()，避免后端运行时 test token 被清除（兼容后端运行场景）|
| `前端页面/ai-picturebook/parent.html` | api.js 版本号 v2→v3 |
| `前端页面/ai-picturebook/upgrade.html` | api.js 版本号 v3→v4 |
| `前端页面/ai-picturebook/reader.html` | api.js 版本号 v3→v4 |
| `前端页面/ai-picturebook/home.html` | api.js 版本号 v7→v8 |
| `静态页面/` | 以上所有文件同步复制 |

**关键设计**：
- 测试账号 `test / test123456` 在 `doLogin` 中先于 API 调用拦截，后端无论是否运行都不会走真实登录
- mock token 以 `test-token-` 前缀标识，api.js 的 401 处理跳过 test token 的 clearAuth，防止后端运行时返回 401 清除会话
- 后端未运行时：API 调用走 catch 分支返回 `{code:500, msg:'网络异常'}`，不触发 clearAuth，token 持久保持，页面正常加载
- 后端运行时：API 调用返回 401 但不清除 test token，页面加载但 API 数据为空（显示默认值）
- 测试账号提示框仅出现在 login.html，标明"仅未部署后端时使用"

**验证结果**（v1.0 前端 8765，后端未运行）：
- login.html 显示测试账号提示框 ✅
- test/test123456 登录 → 跳转 home.html，显示"测试家长" ✅
- parent.html 正常加载，显示成长档案、内容筛选等模块 ✅
- upgrade.html 正常加载，显示会员套餐、支付方式 ✅
- reader.html?bookId=1 正常加载，显示伴读界面 ✅

**同步到静态页面**：
- `静态页面/` 下 login.html、api.js、parent.html、upgrade.html、reader.html、home.html 均已同步

---

## 2026-09-15（傍晚）

### 角色联动：创造角色 ↔ 和角色聊天

**问题背景**：在 studio.html 中，"创造角色"区域选择的故事伙伴不会同步到"和角色聊天"区域——聊天标题和初始消息始终硬编码为"森林小狐"和🦊，切换角色后聊天区不跟随变化。

**修改清单**：

| 文件 | 改动 |
|------|------|
| `前端页面/ai-picturebook/studio.html` | 1) 聊天标题 `<span>` 加 `id="chatRoleName"`，方便 JS 动态更新角色名。2) 新增 `updateChatForRole(name)` 函数：更新 `#chatRoleName` 文本 → 从 `#avatar` 获取当前头像（emoji 或 img）→ 按角色名查 `greetings` 映射返回角色专属问候语 → 重置 `#messages` innerHTML → 清空 `#chatInput`。3) `pickRole()` 末尾调用 `updateChatForRole(name)`，确保每次选角色时聊天区同步刷新。4) `confirmCustomRole()` 末尾调 `pickRole`，自定义角色也触发同步。5 个预设角色的问候语：森林小狐→"你今天想去哪里探险？我已经准备好耳朵啦。"、星星兔→"今晚的星星好亮呀，你想聊点什么？"、海底鲸→"海底的世界好大好大，你想和我聊什么呢？"、云朵龙→"我刚刚从云朵上飞下来，你想聊什么呀？"、月亮猫→"喵～月光下的夜晚最适合聊天了，你想说什么？"；自定义角色→"你好呀！我是{name}，我们一起聊聊天吧～" |

**关键设计**：
- `updateChatForRole` 在 `pickRole` 末尾调用，此时 `#avatar` 已被 `pickRole` 更新为新角色的头像（emoji 或 AI 生成 img），所以聊天区的初始消息头像与角色预览区一致
- 切换角色时重置聊天消息（清空历史对话），避免上一个角色的对话残留
- `chatSessionMap` 按 roleId 缓存会话，切换角色后新角色会创建新会话（或走 mock fallback）

**验证结果**（v1.0 前端 8765）：
- 点击"星星兔"→ 切换到"和角色聊天"：标题"和 星星兔 聊一聊"，初始消息"🐰 今晚的星星好亮呀，你想聊点什么？" ✅
- 点击"海底鲸"→ 切换到"和角色聊天"：标题"和 海底鲸 聊一聊"，初始消息"🐳 海底的世界好大好大，你想和我聊什么呢？" ✅

**同步到静态页面**：
- `静态页面/studio.html` 已与源文件同步（Grep 确认 `updateChatForRole` 和 `chatRoleName` 均存在）

---

## 2026-09-15（下午）

### AI 接入预备：和角色聊天 + 语音识别

**目标**：让 studio.html 的"和角色聊天"和语音识别功能具备完整 AI 接入链路（即使后端当前为 mock 实现），不再使用纯前端 mock。

**修改清单**：

| 文件 | 改动 |
|------|------|
| `前端页面/ai-picturebook/api.js` | 1) 修正 `chat` API 路径与后端 `AiChatController` 对齐：`createSession(roleId)` → `POST /api/chat/session/{roleId}`；`sendMessage` → `POST /api/chat/send` 带 `{sessionId, content, roleId, messageType}`；`messages(sessionId)` → `GET /api/chat/{sessionId}/messages`。2) 在导出的 `api` 对象上新增 `BASE_URL` 字段，供 studio.html 的 `speak()` 直接 fetch 文件上传用 |
| `前端页面/ai-picturebook/studio.html` | 1) `<head>` 引入 `<script src="api.js?v=9">`。2) 脚本顶部新增 `ROLE_ID_MAP`（森林小狐=1、星星兔=2、海底鲸=3、云朵龙=4、月亮猫=5）和 `chatSessionMap`（缓存会话 ID）、`speakMediaRecorder/speakChunks/speakStream` 录音状态。3) `sendChat()` 重写：插入用户消息 + "正在思考"占位 → `chatSendToBackend()` 调 `API.chat.createSession` → `API.chat.sendMessage`，成功显示后端 AI 回复，失败/未登录走 `mockAiReply()` 本地 fallback。4) `speak()` 重写：点击开始录音（`navigator.mediaDevices.getUserMedia` + `MediaRecorder`），再点一次结束录音 → `uploadAndRecognize()` 调 `fetch(API.BASE_URL+'/api/file/upload')` 上传 → `API.ai.asr(audioUrl, 'zh-CN')` 识别 → 把识别文本填入 `chatInput`。5) 新增 `mockAiReply/escapeHtml/stopSpeakRecording/uploadAndRecognize/resetSpeakBtn` 辅助函数 |

**关键设计**：
- 角色名 → ID 映射写死在前端（后端 `data.sql` 初始角色 ID 1-5）；自定义角色无 ID，toast 提示"暂未接入 AI 对话"
- 未登录或 API 不可用时，sendChat 走 `mockAiReply` 本地回复（按"你好/故事/害怕"等关键词返回鼓励性话术），保证 UX 不中断
- 录音采用 webm 格式，上传到 `/api/file/upload`，后端返回 url 后传给 `/api/ai/asr/recognize`
- 录音按钮点击后变 loader 图标，识别完成后恢复原图标

**验证结果**（v1.0 前端 8765 + 后端 8080）：
- studio.html 加载正常，API/ChatApi/BASE_URL/ROLE_ID_MAP/sendChat/speak 全部就绪
- sendChat 测试：输入"你好" → AI 回复"嗨！我是森林小狐，很高兴认识你！"（未登录走 mock fallback）；输入"我害怕黑" → 回复"没关系，勇敢不是不害怕，我陪着你呢！"
- speak 测试：toast 显示"正在聆听，说完再点一次结束"，按钮变 loader 图标（沙箱无麦克风权限，真实浏览器可正常录音）

**同步到静态页面**：
- `静态页面/api.js` 和 `静态页面/studio.html` 已与 v1.0 源文件同步（文件 hash 一致）
- 静态页面 (8765) 测试 sendChat 通过：输入"我害怕黑" → AI 回复"没关系，勇敢不是不害怕，我陪着你呢！"

**后端 mock 状态**（待 M14 接入真实 AI 服务商）：
- `AiChatService.generateAiReply`：按关键词返回鼓励性回复（你好/故事/害怕/开心/为什么/默认随机）
- `AsrServiceImpl.recognize`：从 10 条儿童常用语中随机返回，置信度 0.85-0.98
- `TtsServiceImpl`：返回占位 mp3 URL
- `VoiceCloneServiceImpl`：提交后异步 3 秒变 success

---

## 2026-09-15

### 安全加固：清除硬编码个人信息与后门账号

**问题背景**：未登录访问 `parent.html` 时显示了硬编码的"林小满家长 / 138****8888 / 家庭版 / 2026-12-31"等虚构个人信息；多个页面隐藏了 `demo / 123456`、`kid001 / 123456` 后门自动登录逻辑和体验账号提示。

**修改清单**：

| 文件 | 改动 |
|------|------|
| `前端页面/ai-picturebook/home.html` | 未登录态侧边栏家长信息块：`家/家长/免费版` → `客/未登录/点击登录账户`；链接 `parent.html` → `login.html`；`renderHome` 未登录时跳过家长信息字段更新，避免被 DEFAULT_HOME 伪信息覆盖 |
| `前端页面/ai-picturebook/parent.html` | 移除 `ensureLogin()` 中的 `demo/123456` 后门自动登录，改为未登录跳转 `login.html`；初始化守卫 `if (!ok) return` 避免数据加载失败露出 fallback；清除所有硬编码：侧边栏 `林/林小满家长/家庭版·3位成员`、Header `林小满家长/林`、成长档案标题 `林小满的成长档案`、账号信息 `林小满家长/138****8888/家庭版/2026-12-31` → 全部改为 `·/家长/未登录/--`；表单 placeholder `如：林小满` → `请输入昵称`；`loadUserInfo`/`loadAccountInfo` fallback 默认值改为 `家长` 或 `--` |
| `前端页面/ai-picturebook/library.html` | 侧边栏 `林小满家长/林` → `未登录/·`，链接 `parent.html` → `login.html`；`loadUserInfo` fallback `林小满家长` → `家长` |
| `前端页面/ai-picturebook/login.html` | 删除"体验账号：demo / 123456"提示框 |
| `前端页面/ai-picturebook/child-login.html` | 删除"体验账号：kid001 / 123456"提示框 |
| `前端页面/ai-picturebook/reader.html` | 移除 `ensureReaderLogin()` 中 `demo/123456` 后门自动登录，改为未登录跳转 `login.html`；`init()` 守卫 `if (!ok) return` |

### 修复：绘本库"检查后端状态"错误

**问题背景**：访问 `library.html` 显示"检查后端状态"错误链接，绘本列表无法加载。

**根因**：
1. 后端服务未启动（`localhost:8080` 无监听）
2. `api.js` 中 `BASE_URL = ''`（同源部署配置），开发环境前端在 8765、后端在 8080，请求打到静态服务器返回 404 "File not found"

**修改清单**：

| 文件 | 改动 |
|------|------|
| `前端页面/ai-picturebook/api.js` | `BASE_URL = ''` → `'http://localhost:8080'`（开发环境前后端分端口部署需显式指向后端，生产环境同源部署时改回 `''`） |
| `前端页面/ai-picturebook/library.html` | `<script src="api.js">` → `<script src="api.js?v=8">`（破缓存，确保浏览器加载新版 api.js） |

**验证结果**：
- 后端构建+启动：`mvn clean package -DskipTests` + `java -jar picture-book-backend-1.0.0.jar`，监听 8080，H2 内存数据库，4 本绘本数据正常返回
- 绘本库页面加载 4 本书：小熊和最勇敢的风 / 月亮邮差的礼物 / 云朵鲸鱼的心情 / 藏在云朵里的小房子
- 未登录访问 `parent.html` / `reader.html` 自动跳转 `login.html`
- `login.html` / `child-login.html` 不再暴露任何体验账号信息

### 工程化：新增一键启动脚本与开发日志

- 新增 `开发日志.md`：本文件，记录每次更新（按日期倒序）
- 新增 `启动.ps1`：PowerShell 启动脚本，逻辑：检查后端端口 → 未运行则构建+启动 jar → 检查前端端口 → 未运行则启动 python http.server → 打开浏览器
- 新增 `一键启动.bat`：双击启动器，调用 `powershell -ExecutionPolicy Bypass -File 启动.ps1`
- 配置项（在 `启动.ps1` 顶部可改）：`$BackendPort=8080`、`$FrontendPort=8765`、`$BackendJar`、`$MvnCmd`、`$PythonCmd`、`$FrontendDir`
- 后端日志：`backend-out.log` / `backend-err.log`

### 复制静态页面到独立文件夹

- 新增 `静态页面/` 文件夹，从 `前端页面/ai-picturebook/` 复制全部 9 个文件（api.js、child-login.html、home.html、library.html、login.html、parent.html、reader.html、studio.html、upgrade.html）
- 联动验证通过（临时端口 8766 测试）：
  - 首页加载正常，API 已加载，侧边栏显示"点击登录账户"
  - 绘本库正常加载 4 本书（小熊和最勇敢的风等），后端 API 联动正常
  - 未登录访问 `parent.html` / `reader.html` 自动跳转 `login.html`
- `启动.ps1` 中 `$FrontendDir` 改为 `静态页面`，一键启动现在启动新目录的前端服务
