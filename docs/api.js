 /** * 小芽绘本 AI 伴读成长空间 - 公共 API 封装 * * 维护人：Agent-5（公共文件，统一维护） * 其他 Agent 需加方法时在任务看板写「公共文件请求」段落 * * 功能： *   - 统一 Token 管理 *   - 统一错误处理 *   - 封装所有用户端与管理端 API */const API = (function () {  // ============================================================  // 配置  // ============================================================  const BASE_URL = 'https://circumstances-standards-cookbook-washer.trycloudflare.com'; // 生产环境  const TOKEN_KEY = 'picturebook_token';  const USER_KEY = 'picturebook_user';  // ============================================================  // Token 管理  // ============================================================  function getToken() {    return localStorage.getItem(TOKEN_KEY) || '';  }  function setToken(token) {    localStorage.setItem(TOKEN_KEY, token);  }  function getUser() {    try {      return JSON.parse(localStorage.getItem(USER_KEY) || '{}');    } catch (e) {      return {};
