# 小芽绘本 v1.0 — 部署与接入指南

> 本文档面向**部署本系统**和**接入真实 AI/支付服务**的开发者或 Agent。涵盖环境要求、构建、启动、前端联调、数据库切换、AI/API 接入、生产加固、运维排障全流程。
>
> - 项目版本：v1.0（未接入 AI，所有 AI 能力为 Mock 占位）
> - 技术栈：Spring Boot 3.2.5 + MyBatis Plus + H2/MySQL + Redis + JWT
> - 前端：纯静态 HTML + 原生 JS + Tailwind CSS（CDN）
> - 打包路径：`小芽绘本v1.0_未接入AI/`

---

## 目录

1. [环境要求](#1-环境要求)
2. [快速启动（开发环境）](#2-快速启动开发环境)
3. [项目结构说明](#3-项目结构说明)
4. [配置文件详解](#4-配置文件详解)
5. [数据库切换（H2 → MySQL）](#5-数据库切换h2--mysql)
6. [前端部署](#6-前端部署)
7. [AI/API 接入指南](#7-aiai-接入指南)
8. [支付接入](#8-支付接入)
9. [安全加固（生产环境）](#9-安全加固生产环境)
10. [测试账号与接口验证](#10-测试账号与接口验证)
11. [常见问题排查](#11-常见问题排查)
12. [运维与监控](#12-运维与监控)

---

## 1. 环境要求

### 1.1 必需环境

| 组件 | 版本要求 | 说明 |
|---|---|---|
| **JDK** | 17+（推荐 JDK 22） | Spring Boot 3.2.5 最低要求 JDK 17 |
| **Maven** | 3.8+（推荐 3.9.9） | 构建工具 |
| **Redis** | 5.0+（推荐 7.x） | TTS 缓存、Session、限流；未启动时自动降级 |

### 1.2 可选环境（生产环境推荐）

| 组件 | 版本 | 用途 |
|---|---|---|
| **MySQL** | 8.0+ | 生产数据库（开发默认 H2 内存库） |
| **Nginx** | 1.20+ | 反向代理 + 静态资源分发 + HTTPS |
| **OSS/MinIO** | - | 文件存储（开发默认本地 `uploads/`） |

### 1.3 端口规划

| 端口 | 服务 | 说明 |
|---|---|---|
| 8080 | 后端 API + 静态页面 | Spring Boot 内嵌 Tomcat |
| 6379 | Redis | 缓存服务 |
| 3306 | MySQL（可选） | 生产数据库 |
| 80/443 | Nginx（可选） | 反向代理 |

---

## 2. 快速启动（开发环境）

### 2.1 一键启动流程

```bash
# 1. 进入后端目录
cd 小芽绘本v1.0_未接入AI/picture-book-backend

# 2. 构建打包（跳过测试加速）
mvn clean package -DskipTests

# 3. 启动 Redis（如已安装）
redis-server

# 4. 启动后端
java -jar target/picture-book-backend-1.0.0.jar

# 5. 浏览器访问
# 首页：       http://localhost:8080/home.html
# 家长登录：   http://localhost:8080/login.html
# 孩子登录：   http://localhost:8080/child-login.html
# H2 控制台：  http://localhost:8080/h2-console
# Swagger 文档：http://localhost:8080/swagger-ui/index.html
```

### 2.2 Windows 环境 Maven/JDK 路径示例

如果系统未配置环境变量，可直接用绝对路径：

```powershell
# Maven（参考路径）
& "D:\RuoYi\apache-maven-3.9.9\bin\mvn.cmd" clean package -DskipTests

# JDK（参考路径）
$env:JAVA_HOME = "C:\Program Files\Java\jdk-22"

# 启动
& "C:\Program Files\Java\jdk-22\bin\java" -jar target\picture-book-backend-1.0.0.jar
```

### 2.3 快速验证

启动后，访问以下地址确认服务正常：

| 验证项 | URL | 预期结果 |
|---|---|---|
| 首页 | `GET /home.html` | 200，显示绘本首页 |
| 登录接口 | `POST /api/auth/login` | 返回 JWT token |
| H2 控制台 | `GET /h2-console` | H2 数据库 Web 控制台 |
| API 文档 | `GET /swagger-ui/index.html` | Swagger UI |

---

## 3. 项目结构说明

```
小芽绘本v1.0_未接入AI/
├── API接入注意事项.md           ← AI/API 接入速查（简版）
├── 部署与接入指南.md             ← 本文档（详细版）
├── picture-book-backend/         ← 后端工程
│   ├── pom.xml                   ← Maven 依赖配置
│   ├── src/
│   │   ├── main/
│   │   │   ├── java/com/picturebook/
│   │   │   │   ├── PictureBookApplication.java   ← 启动类
│   │   │   │   ├── ai/                          ← AI 能力模块
│   │   │   │   │   ├── controller/               ← TTS/ASR/声音复刻/AI资源 等接口
│   │   │   │   │   ├── service/impl/            ← TtsServiceImpl/AsrServiceImpl/VoiceCloneServiceImpl 等（Mock）
│   │   │   │   │   ├── domain/                  ← VoiceClone/ChatSession/StoryCreation 等
│   │   │   │   │   ├── dto/                     ← 请求 DTO
│   │   │   │   │   └── mapper/                  ← MyBatis Mapper
│   │   │   │   ├── book/                        ← 绘本库模块
│   │   │   │   ├── user/                        ← 用户/家庭/孩子/订单/习惯 模块
│   │   │   │   ├── admin/                       ← 管理后台模块
│   │   │   │   ├── common/                      ← 公共工具（JWT/AES/异常/常量）
│   │   │   │   └── framework/                   ← 框架配置（Security/Redis/JWT Filter/文件存储）
│   │   │   └── resources/
│   │   │       ├── application.yml              ← 主配置
│   │   │       ├── application-test.yml         ← 测试环境配置
│   │   │       ├── schema.sql                   ← 建表脚本（H2/MySQL 兼容）
│   │   │       ├── data.sql                     ← 初始化数据
│   │   │       └── static/                      ← 前端静态页面（与前端目录镜像）
│   │   └── test/                                ← 单元测试 + 集成测试
│   ├── sql/picture_book_schema.sql              ← MySQL 独立建表脚本
│   └── uploads/                                  ← 文件上传目录
└── 前端页面/ai-picturebook/      ← 前端源码（独立目录，便于单独部署）
    ├── api.js                   ← 公共 API 封装（Token 管理、请求封装）
    ├── home.html                ← 首页
    ├── login.html               ← 家长登录/注册
    ├── child-login.html         ← 孩子登录
    ├── reader.html              ← 绘本伴读（TTS/ASR/音色切换）
    ├── parent.html              ← 家长管理（孩子/会员/订单）
    ├── upgrade.html             ← 会员升级
    ├── studio.html              ← AI 故事创作
    └── library.html             ← 绘本库
```

---

## 4. 配置文件详解

配置文件：`picture-book-backend/src/main/resources/application.yml`

### 4.1 核心配置段

```yaml
server:
  port: 8080                       # 服务端口

spring:
  datasource:
    driver-class-name: org.h2.Driver  # 开发用 H2，生产改 com.mysql.cj.jdbc.Driver
    url: jdbc:h2:mem:picture_book     # H2 内存库
    username: sa
    password:
  data:
    redis:
      host: localhost               # Redis 地址
      port: 6379
  servlet:
    multipart:
      max-file-size: 50MB           # 上传文件大小限制
      max-request-size: 100MB
```

### 4.2 JWT 认证配置

```yaml
picturebook:
  jwt:
    secret: XiaoyaPictureBookJwtSecret2026ForAgingReadingSystem  # 生产必须改！
    expire: 86400                   # Token 有效期（秒），默认 24 小时
    header: Authorization
    prefix: "Bearer "
```

### 4.3 AI 能力配置（当前均为 mock）

```yaml
picturebook:
  ai:
    llm:                            # 大语言模型
      provider: mock                 # ← 接入真实服务时改为 dashscope/deepseek/openai
      model: qwen2.5
      api-url: https://dashscope.aliyuncs.com
      api-key: ${AI_LLM_KEY:}        # 环境变量注入
    tts:                            # 语音合成
      provider: mock                 # ← 改为 volcengine/aliyun/azure
      default-voice: gentle_female
    asr:                            # 语音识别
      provider: mock                 # ← 改为 volcengine/aliyun/xfyun
    review:                         # 内容审核
      enabled: true
      aes-key: ${PICTUREBOOK_AES_KEY:Xiaoya2026AesKeyForChildData2026}
```

### 4.4 文件存储配置

```yaml
picturebook:
  file:
    storage-type: local              # local / oss / minio
    local:
      base-path: d:/project_work/work/picture-book-backend/uploads  # 生产改绝对路径
      url-prefix: /uploads
    allowed-types: jpg,jpeg,png,gif,webp,mp3,wav,m4a,txt,md
```

### 4.5 防沉迷配置

```yaml
picturebook:
  anti-addiction:
    single-session-minutes: 30      # 单次阅读上限
    daily-minutes: 60               # 每日阅读上限
    night-mode-start: "21:00"       # 夜间模式开始时间
    night-mode-brightness: 30       # 夜间亮度（%）
```

### 4.6 安全白名单（免登录路径）

```yaml
picturebook:
  security:
    whitelist:
      - /api/auth/login
      - /api/auth/register
      - /api/auth/logout
      - /api/auth/child/login
      - /api/auth/admin/login
      - /api/book/list
      - /api/book/detail/**
      - /uploads/**
      - /h2-console/**
      - /*.html
      - /*.js
      - /*.css
```

---

## 5. 数据库切换（H2 → MySQL）

开发环境默认使用 H2 内存数据库，启动时自动执行 `schema.sql` 建表 + `data.sql` 初始化数据。**生产环境必须切换到 MySQL。**

### 5.1 切换步骤

**步骤 1：创建 MySQL 数据库**

```sql
CREATE DATABASE picture_book DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

**步骤 2：修改 `application.yml`**

```yaml
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/picture_book?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: your_mysql_user
    password: your_mysql_password
  sql:
    init:
      mode: always                   # 首次启动建表；建表后改为 never 避免重复
      schema-locations: classpath:schema.sql
      data-locations: classpath:data.sql
```

**步骤 3：使用独立建表脚本（可选）**

项目提供 MySQL 独立建表脚本：`picture-book-backend/sql/picture_book_schema.sql`

```bash
mysql -u root -p picture_book < picture-book-backend/sql/picture_book_schema.sql
```

**步骤 4：初始化数据**

首次启动时 `data.sql` 会自动执行，插入：
- 默认管理员（admin/admin123）
- 系统配置
- AI 模型配置（Mock 占位）
- 5 个 AI 角色
- 3 个故事主题
- 4 本示例绘本
- 测试家庭账号（demo/123456、family/family123）

### 5.2 H2 控制台

开发环境可访问 `http://localhost:8080/h2-console` 查看数据：
- JDBC URL: `jdbc:h2:mem:picture_book`
- User: `sa`
- Password: （空）

---

## 6. 前端部署

### 6.1 方式一：后端内嵌部署（开发推荐）

前端页面已镜像到 `picture-book-backend/src/main/resources/static/`，Spring Boot 自动作为静态资源分发。**无需额外部署前端**，直接访问 `http://localhost:8080/home.html` 即可。

**修改前端页面后，需同步到 static 目录并重新构建：**

```bash
# 复制前端到后端 static 目录（Windows PowerShell）
Copy-Item "前端页面\ai-picturebook\*" "picture-book-backend\src\main\resources\static\" -Recurse -Force

# 重新构建
mvn clean package -DskipTests
```

### 6.2 方式二：Nginx 独立部署（生产推荐）

将 `前端页面/ai-picturebook/` 上传到 Nginx 静态目录，后端单独部署。

**Nginx 配置示例：**

```nginx
server {
    listen 80;
    server_name your-domain.com;

    # 前端静态页面
    location / {
        root /var/www/picturebook;
        index home.html;
        try_files $uri $uri/ /home.html;
    }

    # API 反向代理到后端
    location /api/ {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 上传文件代理
    location /uploads/ {
        proxy_pass http://127.0.0.1:8080;
    }

    # H2 控制台（生产建议关闭）
    # location /h2-console { deny all; }
}
```

### 6.3 前端 API 配置

前端 `api.js` 默认同源部署（`BASE_URL = ''`）。如前后端分离部署，修改 `api.js` 中的 `BASE_URL`：

```javascript
const BASE_URL = 'https://api.your-domain.com';  // 改为后端地址
```

---

## 7. AI/API 接入指南

> **详细接入说明请参阅 [API接入注意事项.md](./API接入注意事项.md)**，此处提供摘要 + 关键接入点速查。

### 7.1 接入清单

| 模块 | 当前状态 | 接入文件 | 推荐服务商 |
|---|---|---|---|
| LLM 对话/故事 | Mock 模板回复 | `ai/service/AiChatService.java` → `generateStoryContent()` / `generateAiReply()` | ① 通义千问 ② DeepSeek ③ OpenAI |
| TTS 语音合成 | Mock 占位 URL | `ai/service/impl/TtsServiceImpl.java` → `synthesize()` | ① 火山引擎 ② 阿里云 CosyVoice ③ Azure |
| ASR 语音识别 | Mock 随机语料 | `ai/service/impl/AsrServiceImpl.java` → `recognize()` | ① 火山引擎 ② 阿里云 ③ 讯飞儿童 |
| 声音复刻 | Mock sleep 模拟 | `ai/service/impl/VoiceCloneServiceImpl.java` → `triggerMockTrain()` | ① 火山引擎 ② Azure ③ 阿里云 |
| AI 配图 | 未实现 | `ai/service/AiChatService.java` → `createStory()` | ① 通义万相 ② 文心一格 ③ DALL-E |
| 内容审核 | Mock 实现 | `ai/service/ContentReviewService.java` | ① 阿里云内容安全 |

### 7.2 接入通用模式

所有 AI 服务的 Mock 实现遵循同一模式：

```
配置开关 (provider=mock) → Mock 分支返回占位数据
                    ↓ 改为真实 provider
                    真实分支调用服务商 API
```

**接入步骤（以 TTS 为例）：**

1. **申请服务商账号**，获取 API Key / App ID
2. **设置环境变量**（不要硬编码到代码）：
   ```bash
   export VOLC_TTS_APP_ID=your_app_id
   export VOLC_TTS_TOKEN=your_access_token
   ```
3. **修改 `application.yml`**：
   ```yaml
   picturebook:
     ai:
       tts:
         provider: volcengine    # 从 mock 改为 volcengine
   ```
4. **修改 Service 实现**：在 `TtsServiceImpl.synthesize()` 中，当 `provider != "mock"` 时调用真实 API
5. **音色映射**：将内置 12 个 `voiceId` 映射到服务商音色 ID
6. **测试验证**：前端 reader.html 选择音色 → 朗读 → 确认返回真实音频

### 7.3 环境变量清单

接入真实服务后，以下环境变量必须配置：

```bash
# LLM
AI_LLM_KEY=sk-xxx

# 火山引擎 TTS/ASR/声音复刻
VOLC_TTS_APP_ID=xxx
VOLC_TTS_TOKEN=xxx
VOLC_ASR_APP_ID=xxx
VOLC_ASR_TOKEN=xxx

# AI 配图
AI_IMAGE_KEY=sk-xxx

# 内容审核
ALIYUN_REVIEW_AK=xxx
ALIYUN_REVIEW_SK=xxx

# 数据加密
PICTUREBOOK_AES_KEY=your_aes_key
```

### 7.4 接入优先级建议

```
LLM → TTS → ASR → 声音复刻 → AI 配图 → 支付 → 内容审核
（影响面最大）                                    （随时可接入）
```

---

## 8. 支付接入

### 8.1 当前状态

`OrderServiceImpl.payOrder()` 直接将订单状态改为已支付（Mock），用于流程验证。

### 8.2 接入位置

```
picture-book-backend/src/main/java/com/picturebook/user/service/impl/OrderServiceImpl.java
```

### 8.3 推荐方案

| 服务商 | 适用场景 | 接入文档 |
|---|---|---|
| **支付宝当面付** | PC 扫码支付 | https://opendocs.alipay.com/open/02nkfg |
| **微信 Native 支付** | PC 扫码支付 | https://pay.weixin.qq.com/docs/merchant/products/native-payment.html |
| **微信 JSAPI 支付** | 微信内 H5 | https://pay.weixin.qq.com/docs/merchant/products/jsapi-payment.html |

### 8.4 支付流程

```
前端 upgrade.html
  → POST /api/order/create    创建订单，返回 orderId
  → POST /api/order/pay       后端调用支付接口，返回支付二维码/跳转URL
  → 用户扫码支付
  → 支付宝/微信异步回调 /api/order/pay/notify  后端验签 → 更新订单状态
  → 前端轮询 GET /api/order/{id}/status       支付成功 → 刷新会员
```

### 8.5 接入要点

1. `MemberOrder` 实体已有 `payType`、`transactionId`、`paidAt` 字段
2. 支付回调接口 `/api/order/pay/notify` 需加入 `SecurityConfig` 白名单
3. 回调必须验签，防止伪造
4. 支付成功后同步更新 `biz_family.member_level` 和 `member_end_at`

---

## 9. 安全加固（生产环境）

### 9.1 必须修改的配置

```yaml
# 1. 修改 JWT Secret（切勿使用默认值）
picturebook:
  jwt:
    secret: ${JWT_SECRET:your_random_64_char_secret_here}

# 2. 修改 AES 加密密钥
picturebook:
  ai:
    review:
      aes-key: ${PICTUREBOOK_AES_KEY:your_random_aes_key}

# 3. 关闭 H2 控制台
spring:
  h2:
    console:
      enabled: false

# 4. 关闭 SQL 日志
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.nologging.NoLoggingImpl

# 5. 切换到 MySQL
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://...
```

### 9.2 网络安全

- 启用 HTTPS（通过 Nginx 配置 SSL 证书）
- 限制 `/h2-console` 仅内网访问
- 配置 CORS 白名单（`SecurityConfig.java` 中当前允许 `*`，生产改为具体域名）
- 限制文件上传类型和大小

### 9.3 数据安全

- 所有 AI API Key 走环境变量注入
- 用户密码使用 BCrypt 加密（已实现）
- JWT Token 24 小时过期（可配置）
- 孩子账户数据加密存储（AES，已实现）

---

## 10. 测试账号与接口验证

### 10.1 测试账号

系统初始化时自动创建以下账号（见 `data.sql`）：

| 角色 | 用户名 | 密码 | 用途 |
|---|---|---|---|
| 管理员 | admin | admin123 | 管理后台 |
| 家长 | demo | 123456 | 体验账号 |
| 家长 | family | family123 | 家庭账号 |
| 孩子 | xiaoman | 123456 | demo 家庭的孩子 |
| 孩子 | kid001 | 123456 | demo 家庭的孩子 |

### 10.2 接口验证

**家长登录：**
```bash
curl -X POST http://localhost:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"demo","password":"123456"}'
```

预期返回：
```json
{
  "code": 200,
  "data": {
    "familyId": 2,
    "nickname": "林小满家(测试)",
    "memberLevel": "family",
    "token": "eyJhbGciOiJIUzM4NCJ9..."
  }
}
```

**首页数据（需带 Token）：**
```bash
curl http://localhost:8080/api/user/home \
  -H "Authorization: Bearer <token>"
```

**孩子登录：**
```bash
curl -X POST http://localhost:8080/api/auth/child/login \
  -H "Content-Type: application/json" \
  -d '{"username":"kid001","password":"123456"}'
```

### 10.3 运行单元测试

```bash
cd picture-book-backend
mvn test
```

测试覆盖：
- `UserServiceTest` — 注册/登录/退出
- `UserControllerTest` — 接口层测试
- `JwtUtilTest` — JWT 生成与验证
- `JwtAuthFilterTest` — 认证过滤器
- `SecurityUtilTest` — 密码加密
- `OrderServiceTrialTest` — 试用会员逻辑
- `AuthFlowIntegrationTest` — 完整认证流程集成测试

---

## 11. 常见问题排查

### 11.1 启动失败

| 现象 | 原因 | 解决方案 |
|---|---|---|
| 端口 8080 被占用 | 其他进程占用 | `netstat -ano \| findstr 8080` 查找并 kill |
| Redis 连接失败 | Redis 未启动 | 启动 Redis；或忽略（系统自动降级） |
| H2 建表失败 | schema.sql 语法错误 | 检查 `schema.sql`，H2 兼容 MySQL 模式 |
| Maven 构建失败 | JDK 版本过低 | 确认 JDK ≥ 17 |

### 11.2 前端问题

| 现象 | 原因 | 解决方案 |
|---|---|---|
| 页面 404 | 前端文件未同步到 static/ | 复制 `前端页面/` 到 `src/main/resources/static/` 后重新构建 |
| 家长名字显示错误 | 前端未引入 api.js 或 Token 键不一致 | 确认页面引入了 `<script src="api.js"></script>` |
| API 请求 401 | Token 未正确携带 | 检查 `api.js` 中 `TOKEN_KEY = 'picturebook_token'` |
| 升级按钮无响应 | 未引入 api.js | 在 `<head>` 中添加 `<script src="api.js"></script>` |

### 11.3 后端问题

| 现象 | 原因 | 解决方案 |
|---|---|---|
| 登录返回"密码错误" | 密码加密不一致 | 确认注册和登录均使用 BCrypt（`SecurityUtil.encryptPassword`） |
| 注册返回"账号已存在" | 账号重复 | 换一个用户名 |
| jar 文件被锁定无法构建 | Java 进程未关闭 | 先 `Stop-Process -Name java -Force` 再构建 |
| 图片/音频上传失败 | 目录权限 | 确认 `uploads/` 目录存在且可写 |

### 11.4 前后端联调问题

**跨域问题：**
- `SecurityConfig.java` 已配置 CORS 允许所有来源
- 生产环境建议改为具体域名

**Token 存储键：**
- 统一使用 `picturebook_token`（api.js 定义）
- 不要在前端页面中自定义 `TOKEN_KEY = 'pb_token'` 等不同键名

---

## 12. 运维与监控

### 12.1 日志

```yaml
logging:
  level:
    com.picturebook: debug         # 开发环境
    org.springframework.security: info
```

生产环境建议改为 `info` 或 `warn`。

### 12.2 健康检查

- `GET /home.html` → 200 表示服务正常
- `GET /api/book/list` → 200（白名单接口，无需认证）表示数据库正常

### 12.3 性能优化建议

- **TTS 缓存**：已实现 Redis 缓存，同一文本+音色的合成结果缓存 24 小时
- **数据库连接池**：H2 默认连接池，MySQL 建议配置 HikariCP
- **静态资源 CDN**：Tailwind CSS 和 Iconify 通过 CDN 加载，生产建议下载本地化
- **JVM 参数**：建议 `-Xmx512m -Xms256m`（小型应用）

### 12.4 备份

- **H2**：内存库，重启数据丢失，仅用于开发
- **MySQL**：定期 `mysqldump` 备份
- **上传文件**：备份 `uploads/` 目录

---

## 附录：快速部署 Checklist

部署前逐项确认：

- [ ] JDK 17+ 已安装
- [ ] Maven 3.8+ 已安装
- [ ] Redis 已启动（或接受降级）
- [ ] `application.yml` 配置已修改（JWT Secret、数据库、文件路径）
- [ ] 前端页面已同步到 `src/main/resources/static/`
- [ ] `mvn clean package -DskipTests` 构建成功
- [ ] `java -jar target/picture-book-backend-1.0.0.jar` 启动成功
- [ ] 访问 `http://localhost:8080/home.html` 正常
- [ ] 测试账号 `demo/123456` 登录成功
- [ ] H2 控制台可访问（开发环境）

生产环境额外确认：

- [ ] 切换到 MySQL 数据库
- [ ] 关闭 H2 控制台
- [ ] 修改 JWT Secret 和 AES Key
- [ ] CORS 限制为具体域名
- [ ] 配置 HTTPS
- [ ] AI API Key 通过环境变量注入
- [ ] 备份策略已制定

---

*本文档由 v1.0 打包时生成。如项目结构或配置有变动，请同步更新。*
